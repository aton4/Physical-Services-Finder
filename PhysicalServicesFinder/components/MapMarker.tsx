import React from 'react';
import {
  StyleSheet,
  View,
  Image,
  TouchableOpacity,
  SafeAreaView,
  FlatList,
} from 'react-native';
import { Marker } from "react-native-maps";

const MapMarker = () => {
  
}

export default MapMarker;